// Array --> An array in JavaScript is a special data structure that can hold multiple values in a single variable. 
// --> Arrays can store numbers, strings, objects, or even other arrays.
// --> array is a special variable which can hold a multiple values.
// --> An array can hold many values under a single name, and you can access the values by referring to an index number.
// --> Starting with a ( 0 ) Index
let array1 = ["Bhagat", "Ajay", "manish", "Sumit", "Ganesh"] 

// 1. accessing array element
let access = array1[3]
console.log(access)  // output :- Sumit

// using new Array()
let colors = new Array("Red", "Green", "Blue");
console.log(colors);  // ✅ Output: ["Red", "Green", "Blue"]

// --> Indexing starts from 0.
// --> Access elements using array[index].
